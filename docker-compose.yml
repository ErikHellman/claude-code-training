services:
  claude-code:
    image: ghcr.io/YOUR_GITHUB_USERNAME/claude-code-training:latest
    stdin_open: true
    tty: true
    volumes:
      - claude-auth:/home/student/.claude
      - ./workspace:/workspace

volumes:
  claude-auth:
