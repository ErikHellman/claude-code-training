services:
  claude-code:
    image: ghcr.io/erikhellman/claude-code-training:latest
    stdin_open: true
    tty: true
    volumes:
      - claude-auth:/home/student/.claude
      - ./workspace:/workspace

volumes:
  claude-auth:
